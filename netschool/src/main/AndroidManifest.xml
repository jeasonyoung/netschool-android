<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="auto"
    package="com.examw.netschool">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application android:name=".app.AppContext"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@drawable/app_logo"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        tools:replace="android:icon,android:theme">
        
        <!-- 启动Activity -->
        <activity android:name=".StartActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <category android:name="android.intent.category.LAUNCHER"/>
                <data android:scheme="http"
                    android:host="@string/app_host"/>
            </intent-filter>
        </activity>
        
        <!-- 引导页Activity -->
        <activity android:name=".GuideActivity"
            android:screenOrientation="portrait"/>
        
        <!-- 学员登录Activity -->
        <activity android:name=".LoginActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustUnspecified|stateHidden"/>
        
        <!-- 主界面Activity -->
        <activity android:name=".MainActivity"
            android:screenOrientation="portrait"/>
        
        <!-- 我的课程Activity -->
        <activity android:name=".MyCourseActivity"
            android:screenOrientation="portrait"/>
        
        <!-- 我的课程资源Activity -->
        <activity android:name=".MyCourseLessonActivity"
            android:screenOrientation="portrait"/>

        <!-- vitamio 主播放器(优先) -->
        <activity android:name=".PrimaryPlayActivity"
            android:configChanges="orientation|screenSize|smallestScreenSize|keyboard|keyboardHidden|navigation"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:screenOrientation="landscape"/>

        <!-- android原生播放器(第二播放器)-->
        <activity android:name=".NativePlayActivity"
            android:configChanges="orientation|screenSize|smallestScreenSize|keyboard|keyboardHidden|navigation"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:screenOrientation="landscape"/>
         
        <!-- 下载Activity -->
        <activity android:name=".DownloadActivity"
            android:screenOrientation="portrait"/>
         
        <!-- 免费体验Activity -->
        <activity android:name=".FreeExperienceActivity"
            android:screenOrientation="portrait"/>
         
        <!-- 答疑主题Activity -->
        <activity android:name=".AnswerActivity"
            android:screenOrientation="portrait"/>

        <!-- 答疑明细Activity -->
        <activity android:name=".AnswerDetailActivity"
            android:screenOrientation="portrait"/>

        <!-- 答疑提交Activity -->
        <activity android:name=".AnswerSubmitActivity"
            android:screenOrientation="portrait"/>
        
        <!-- 播放记录 -->
        <activity android:name=".PlayRecordActivity"
            android:screenOrientation="portrait"/>
         
        <!-- 意见反馈 -->
        <activity android:name=".SuggestActivity"
            android:screenOrientation="portrait"/>
         
        <!-- vitamio初始化 -->
        <activity android:name="io.vov.vitamio.activity.InitActivity"
            android:configChanges="orientation|screenSize|smallestScreenSize|keyboard|keyboardHidden|navigation"
            android:launchMode="singleTop"
            android:theme="@style/Theme.AppCompat.NoActionBar"
           	android:windowSoftInputMode="stateAlwaysHidden"
            tools:replace="android:theme"/>

        <!-- 分发渠道 -->
        <meta-data android:name="channel" android:value="360"/>
    </application>
</manifest>